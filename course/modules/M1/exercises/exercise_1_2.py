"""Самостоятельная практика 1.2 — Проверка модели.

1. Прогноз до кода: Предскажите, согласуется ли смесь с одной логнормалой. Какие ошибки подгонки важны для бюджета, а какие — для типичного чека?
2. Задание: Оцените MoM/MLE одной логнормалы, implied mean/median; постройте QQ обеих моделей. Выберите, какие выводы нельзя делать без проверки модели.
3. Сдача: расчёт/график, 3–5 предложений вывода и список допущений.
Критерии: правильный estimand и единица; формулы/CI; различение «не обнаружили»
и «нет эффекта»; решение соответствует данным. Ответ «данных недостаточно» допустим.
Разбор открывайте ПОСЛЕ попытки: ../lesson-1.2.md и ../practice/practice_1_2.py.
"""
import numpy as np
from scipy import stats

rng = np.random.default_rng(102)
data = np.r_[rng.lognormal(np.log(900), .55, 1000), rng.lognormal(np.log(15000), .6, 20)]


def analyze():
    # TODO: выполните расчёты задания и верните именованные результаты.
    raise NotImplementedError("Сначала запишите прогноз, затем заполните analyze")


if __name__ == "__main__":
    print(__doc__)
    print(analyze())
